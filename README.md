# Image Shatter (Pygame)

Демо: картинка в безрамочном окне, фон прозрачный (Windows). Нажмите **Space** — изображение разбивается на фрагменты и «взрывается» с гравитацией, через ~2 сек. окно закрывается.

## Требования

* Python 3.9+
* Windows 10/11 (для прозрачности)
* `pip install pygame`

## Запуск

1. Укажите путь к картинке в `IMAGE_PATH`.
2. Запустите:

```bash
python main.py
```

**Управление:** `Space` — взрыв, `Esc` — выход.

## Настройки

* `grid = 8` — размер сетки (меньше = крупнее, быстрее).
* В классе `Fragment`: подстройте диапазоны `speed`, `gravity`, `rotation_speed`.

## Примечания

* Прозрачность через WinAPI, на Linux/macOS запустится без неё (закомментируйте блок `ctypes`).
* Если лагает — уменьшите `grid` или размер изображения.

Готово к эффектному «взрыву» пикселей 💥
